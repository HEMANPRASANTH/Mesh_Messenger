package com.bitchat.android.ui.theme

import androidx.compose.ui.graphics.Brush
import androidx.compose.ui.graphics.Color

// Liquid Glass Palette

// Light Mode Colors
val LiquidWhite = Color.White
val LiquidBlack = Color.Black // Amoled Black
val LiquidBlue = Color(0xFF007AFF) // High visibility Blue for borders/lines

// Dark Mode Colors
val LiquidIceBlue = Color(0xFF40C4FF) // Glowing Ice Blue for borders/lines
val LiquidTextWhite = Color.White

// Translucent Surfaces (Dynamic)
val LiquidGlassLight = Color.White.copy(alpha = 0.5f)
val LiquidGlassDark = Color.Black.copy(alpha = 0.5f)
